# Workflow Map: [Session Title]

> Auto-generated by the intelligence spine from `recordings/[session-id]/transcript.json` and keyframes. Filed at `recordings/[session-id]/workflow-map.md`. Also linked from the matching folder under `workflows/library/[dynamic-path]/`.

## Session metadata
- Session ID: [session-id]
- Recorded: [YYYY-MM-DD HH:MM]
- Duration: [mm:ss]
- Deal type classification: [auto-classified by the intelligence spine]
- Confidence: [0.00 - 1.00]
- Routing target under workflows/library/: [dynamic path]

## Operator intent
One sentence summary of what the operator was attempting in this session. Extracted from the opening 30 seconds of transcript.

## Tools observed
Every application, website, or document touched during the recording. Captured from screen frames and transcript.

| Tool | First seen | Role in workflow |
|---|---|---|
| ... | mm:ss | ... |

## Steps
Numbered operator actions. Each step links to the transcript span and keyframe that proves it happened.

1. [Action], transcript [start-end], keyframe `keyframes/[name].jpg`
2. ...

## Components identified
The buildable units this workflow decomposes into. Each maps to a component in the PRD/handoff.

| Component | Category | Encoded logic (short) | Scope |
|---|---|---|---|
| ... | [ui_ux/data_integration/codifiable_logic/ai_intelligence/open_question] | ... | [v1/Phase 2] |

## Decisions
Every point where the operator paused, compared options, or referenced external knowledge. Each carries the heuristic applied and the reasoning behind it.

- [Decision], heuristic applied: [...], rationale: [why the rule holds]

## Operator-assisted judgment
Visual or experiential calls the operator made that are not trivial to automate. Each carries a v1 operator-assisted fallback.

- [Judgment], v1 fallback: [one-click operator tag], Phase 2: [automation path]

## Data sources
External datasets, APIs, or documents the operator pulled from.

## Heuristics surfaced
Judgment rules the operator articulated in the transcript. These feed `memory/patterns.md`.

## Open loops
Anything the operator did not resolve in this session. Carried into the PRD as open questions.

## Pattern comparison
Output of the intelligence spine's compare-against-library step. Either "new pattern" or "extends `[existing pattern path]`".

## Next artifacts
- `prds/[session-id]/PRD.md`
- `prds/[session-id]/handoff-prompt.md`
- `workflows/library/[dynamic-path]/` (created or updated)
